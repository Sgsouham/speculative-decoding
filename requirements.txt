# Canonical environment is managed by uv (see pyproject.toml + uv.lock).
# This file is a convenience/fallback reference only — do not install on Windows (CPU-only torch).
# Version pins mirror pyproject.toml `dependencies` + the `dev` dependency group
# (pytest is required to run the test suite; the Aug 7 venv-rebuild note flagged it missing).
torch>=2.13.0
transformers==5.14.1
accelerate>=1.14.0
pytest>=9.1.1
