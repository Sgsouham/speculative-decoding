# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# uv / virtualenv
.venv/

# Generated benchmark outputs (results/ tracked via .gitkeep; JSON runs are ephemeral)
results/*.json

# Draft-head data (WikiText-2 corpus + feature cache: GBs of generated tensors — never commit)
data/

# Private working docs (dev history, session state, planning) — never publish
docs/internal/

# OS noise
.DS_Store
Thumbs.db

# HF cache / model downloads
.hf_cache/
